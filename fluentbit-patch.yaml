spec:
  template:
    spec:
      containers:
      - name: fluent-bit
        resources:
          requests:
            cpu: "100m"
            memory: "50Mi"
          limits:
            cpu: "200m"
            memory: "100Mi"
